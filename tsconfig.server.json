{
    "extends": "./tsconfig.json",
    "compilerOptions": {
      "noEmit": false, // Permitir la generación de archivos .js
      "outDir": "./dist/", // Directorio de salida para los archivos compilados del backend
      "composite": true
    },
    "include": ["server/server.ts"] // Solo incluir server.ts
  }